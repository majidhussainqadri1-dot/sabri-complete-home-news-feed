(() => {
	'use strict';
	const root = document.getElementById('sabri-phase5-admin-app');
	if (!root) return;
	root.textContent = 'Operational data loads only after authenticated REST authorization.';
})();
